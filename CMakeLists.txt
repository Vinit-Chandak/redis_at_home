cmake_minimum_required(VERSION 3.10)
project(redis_at_home)

# Add the source directory (assuming your code is in src/)

# Add the tests directory (assuming your tests are in tests/)
add_subdirectory(tests)
